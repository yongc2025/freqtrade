# 回测&实盘指令使用指南

本文基于 `docs/zh_CN` 相关文档整理，目标不是覆盖全部文档，而是从 **回测**、**参数优化**、**实盘/模拟盘运行** 三个维度，提炼出最常用、最能提升使用效率的指令集、用法说明与示例。

适合你当前这种场景：

- 有多个策略文件和多个配置文件
- 需要频繁下载数据、回测、调参、跑模拟盘/实盘
- 希望少翻文档，直接拿命令就能用

---

## 1. 使用前的整体思路

建议把日常工作拆成 4 步：

1. **准备配置 / 策略**
2. **下载数据**
3. **回测 / 参数优化**
4. **模拟盘验证 / 实盘运行 / 结果分析**

推荐目录关注点：

- 策略：`user_data/strategies/`
- 配置：`user_data/*.json`
- 数据：`user_data/data/`
- 回测结果：`user_data/backtest_results/`
- 超参结果：`user_data/hyperopt_results/`
- 图表：`user_data/plot/`
- 日志：`user_data/logs/`

---

## 2. 基础高频命令

### 2.1 配置与策略

#### 创建配置

```bash
freqtrade new-config -c config.json
```

用途：

- 交互式生成基础配置
- 新项目初始化时使用

#### 创建策略模板

```bash
freqtrade new-strategy -s MyStrategy
```

用途：

- 生成策略骨架
- 新策略开发起点

#### 查看可用策略

```bash
freqtrade list-strategies
```

用途：

- 检查 `--strategy` 名称是否正确
- 快速确认策略是否被识别

---

## 3. 数据下载与检查

相关文档来源：

- `docs/zh_CN/数据下载.md`

### 3.1 下载基础数据

#### 按配置下载

```bash
freqtrade download-data --config user_data/config.json
```

说明：

- 使用配置中的交易所、交易对、交易模式等设置
- 最省事，适合日常维护

#### 指定交易所下载

```bash
freqtrade download-data --exchange binance
```

说明：

- 未传配置时常用
- 默认会下载较短周期默认数据

#### 指定时间周期

```bash
freqtrade download-data --exchange binance --timeframes 5m 1h
```

说明：

- 常用于同时下载回测主周期和辅助周期数据

#### 指定交易对

```bash
freqtrade download-data --exchange binance --pairs BTC/USDT ETH/USDT XRP/USDT
```

#### 用正则批量下载

```bash
freqtrade download-data --exchange binance --pairs ".*/USDT"
```

说明：

- 适合大范围下载 USDT 市场
- 如需包含非活跃币，可再加 `--include-inactive-pairs`

---

### 3.2 下载期货数据

#### 按期货模式下载

```bash
freqtrade download-data --exchange binance --trading-mode futures --timeframes 1h
```

说明：

- 期货回测必须保证数据是 futures 数据
- 文档说明：期货模式下会自动下载所需 candle types（如 `mark`、`funding_rate`），除非你显式指定 `--candle-types`

#### 结合配置下载

```bash
freqtrade download-data --config user_data/config_momentum_server_v1.json --timeframes 1h
```

适合：

- 直接按现有 live/dry-run 配置补齐同一市场数据

---

### 3.3 指定时间范围下载

#### 最近 N 天

```bash
freqtrade download-data --exchange binance --timeframes 1h --days 120
```

#### 从某天开始到现在

```bash
freqtrade download-data --exchange binance --timeframes 1h --timerange 20250101-
```

#### 补更早的数据

```bash
freqtrade download-data --exchange binance --pairs ETH/USDT BTC/USDT --prepend --timerange 20240101-20250101
```

说明：

- 已有 2025 年数据，想往前补 2024 年时用 `--prepend`

---

### 3.4 检查本地数据

#### 查看已下载数据

```bash
freqtrade list-data
```

#### 显示时间范围

```bash
freqtrade list-data --show-timerange
```

#### 指定目录查看

```bash
freqtrade list-data --datadir user_data/data/binance
```

用途：

- 回测前先确认数据是否齐全
- 检查 futures / spot 是否下错目录或下错模式

---

## 4. 回测指令集

相关文档来源：

- `docs/zh_CN/回测指南.md`
- `docs/commands/backtesting.md`
- `docs/zh_CN/高级回测分析.md`

---

### 4.1 最常用回测命令

#### 最基础回测

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy
```

#### 指定时间周期回测

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --timeframe 5m
```

说明：

- `--timeframe` 可覆盖配置中的周期
- 适合快速测试不同周期

#### 指定时间区间回测

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --timerange 20250101-20250301
```

文档中明确支持：

- `--timerange=-20180131`
- `--timerange=20180131-`
- `--timerange=20180131-20180301`
- `--timerange=1527595200-1527618600`

实战建议：

- 日常优先用日期范围
- 需要更精确时可用 Unix 时间戳

---

### 4.2 期货回测

```bash
freqtrade backtesting --config user_data/config_momentum_server_v1.json --strategy MomentumFusion_V1 --timerange 20250101-
```

注意：

- 配置里必须是 `trading_mode: futures`
- 数据也必须是 futures 数据
- 交易对命名必须符合 CCXT 期货格式：`ETH/USDT:USDT`

---

### 4.3 回测时覆盖关键参数

#### 覆盖最大持仓数

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --max-open-trades 5
```

#### 覆盖单笔金额

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --stake-amount 100
```

#### 覆盖手续费

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --fee 0.001
```

#### 指定起始资金

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --dry-run-wallet 10000
```

用途：

- 模拟不同资金体量
- 校准与模拟盘/实盘更接近的手续费、持仓、仓位设置

---

### 4.4 指定交易对回测

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy -p BTC/USDT ETH/USDT
```

用途：

- 单币诊断
- 小池子快速测试
- Long/Short 逻辑分组排查时很好用

---

### 4.5 回测多策略对比

```bash
freqtrade backtesting --config user_data/config_titan_bt.json --strategy-list MomentumFusion_V1 MomentumFusion_V7  --timerange 20250101-
```

说明：

- 适合做版本对比
- 输出结果可直接比较

---

### 4.6 提高回测精度

#### 使用细粒度周期辅助

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --timerange 20250101- --timeframe-detail 5m
```

说明：

- 主周期可能是 `1h`
- 细分周期可改善回测中止损/离场触发的近似精度
- 文档明确支持 `--timeframe-detail`

适合：

- 止损较紧的策略
- 快进快出策略
- 期货策略

---

### 4.7 保护机制回测

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --enable-protections
```

说明：

- 文档明确：默认回测不启用 protections
- 开启后会明显变慢，但结果更接近实际运行

适合：

- 策略使用 `CooldownPeriod`、`StoplossGuard`、`MaxDrawdown` 等保护机制

---

### 4.8 动态 pairlist 说明

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --enable-dynamic-pairlist
```

文档关键点：

- 只有 **支持 backtesting 的 pairlist handler** 才生效
- 会在每根新 K 线刷新 pairlist

重要限制：

- 不是所有动态 pairlist 都支持回测
- 例如你当前环境里 `VolumePairList`、`AgeFilter` 会报不支持 backtesting

因此实践建议：

- 要可复现：优先用 `StaticPairList`
- 要近似动态：分段静态池 / 自定义离线重建候选池

---

### 4.9 导出回测结果

#### 默认导出交易

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --export trades
```

#### 不导出

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --export none
```

#### 导出信号

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --export signals
```

说明：

- `signals` 是做高级分析的关键
- 会在 `user_data/backtest_results/` 下生成结果文件

#### 指定导出目录

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --export trades --backtest-directory user_data/custom-backtest-results
```

#### 指定导出文件名

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --export trades --backtest-filename my_bt_result.json
```

---

### 4.10 回测结果查看

#### 查看最近一次回测结果

```bash
freqtrade backtesting-show
```

用途：

- 快速复盘刚跑完的结果

#### 按时间维度拆分统计

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --breakdown day week month
```

用途：

- 看日/周/月表现稳定性
- 判断是否只在少数阶段赚钱

#### 缓存控制

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --cache none
```

说明：

- 文档建议在高级分析前避免旧缓存干扰
- 排查“为什么结果没变化”时非常有用

---

## 5. 高级回测分析

相关文档来源：

- `docs/zh_CN/高级回测分析.md`

### 5.1 先导出 signals

```bash
freqtrade backtesting --config user_data/config.json --strategy MyStrategy --timerange 20250101-20250301 --export signals
```

说明：

- 不只是交易结果，还会导出触发信号对应的 dataframe 数据
- 是分析 enter_tag / exit_tag 的前提

---

### 5.2 分析 enter_tag / exit_tag

```bash
freqtrade backtesting-analysis -c user_data/config.json --analysis-groups 0 1 2 3 4 5
```

文档中的分析分组含义：

- `0`：按 `enter_tag` 汇总整体胜率和收益
- `1`：按 `enter_tag` 汇总收益
- `2`：按 `enter_tag + exit_tag` 汇总收益
- `3`：按 `pair + enter_tag` 汇总收益
- `4`：按 `pair + enter_tag + exit_tag` 汇总
- `5`：按 `exit_tag` 汇总收益

这个命令特别适合你这种：

- 一个策略里有多个信号组
- 想知道是谁赚钱、谁亏钱

---

### 5.3 指定历史回测文件分析

```bash
freqtrade backtesting-analysis -c user_data/config.json --analysis-groups 0 2 5 --backtest-filename backtest-result-2025-03-05_20-38-34.zip
```

用途：

- 对历史结果做复盘
- 比较多个版本策略的标签贡献

---

## 6. 图表与可视化

相关文档来源：

- `docs/zh_CN/FreqUI界面.md`
- `docs/zh_CN/图表绘制.md`（可进一步查看）

### 6.1 用 FreqUI 看回测/运行

#### 安装 / 更新 UI

```bash
freqtrade install-ui
```

#### 仅启动 Web 界面服务

```bash
freqtrade webserver --config user_data/config.json
```

说明：

- Webserver 模式下可使用更多工具功能
- 文档说明支持：下载数据、测试 pairlist、回测等

---

### 6.2 运行机器人并通过 UI 监控

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy
```

前提：

- 配置中启用 `api_server`
- 然后访问对应端口（如 `http://127.0.0.1:8080` 或你的自定义端口）

---

## 7. 参数优化（Hyperopt）指令集

相关文档来源：

- `docs/zh_CN/超参数优化.md`
- `docs/zh_CN/高级超参数优化.md`

---

### 7.1 安装优化依赖

```bash
pip install -r requirements-hyperopt.txt
```

说明：

- 非 docker 环境下通常需要
- 没装依赖时 hyperopt 跑不起来

---

### 7.2 最基础超参优化

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --hyperopt-loss SharpeHyperOptLossDaily --spaces buy sell --epochs 100
```

说明：

- `--spaces` 选择要优化的空间
- `--epochs` 指轮数
- `--hyperopt-loss` 指目标函数

---

### 7.3 常用优化空间

文档中明确常见空间包括：

- `buy`
- `sell`
- `enter`
- `exit`
- `roi`
- `stoploss`
- `trailing`
- `protection`

#### 优化买卖点

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces buy sell --hyperopt-loss SharpeHyperOptLossDaily -e 200
```

#### 只优化 ROI / 止损 / 追踪止损

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces roi stoploss trailing --hyperopt-loss SharpeHyperOptLossDaily -e 100
```

#### 优化 protections

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces protection --hyperopt-loss SharpeHyperOptLossDaily -e 100
```

---

### 7.4 并行优化

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces buy sell -e 300 -j -1
```

说明：

- `-j -1` 表示用全部 CPU
- 机器资源紧张时改成 `-j 4` 等更稳

---

### 7.5 指定时间区间优化

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --timerange 20250101-20250331 --spaces buy sell roi stoploss --hyperopt-loss SharpeHyperOptLossDaily -e 300
```

实践建议：

- 优化区间不要太短
- 最好做样本内 / 样本外拆分

---

### 7.6 每轮分析 / 指标生成注意点

文档要点：

- `populate_indicators()` 必须生成所有可能被参数空间使用的指标
- 否则 hyperopt 无法工作

换句话说：

- 即使某个触发器只在部分参数组合里用到，其指标也要提前算好

---

### 7.7 查看优化结果

#### 查看最近结果列表

```bash
freqtrade hyperopt-list -n 20
```

#### 只看较优结果

```bash
freqtrade hyperopt-list --best -n 20
```

#### 查看某条结果详情

```bash
freqtrade hyperopt-show --index 1
```

#### 查看最近一次任务中的结果

```bash
freqtrade hyperopt-show -n -1
```

用途：

- 从结果中提取参数
- 粘贴回策略或 json 参数文件

---

### 7.8 常见实战套路

#### 先粗筛再精调

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces buy sell -e 100 -j -1
```

然后再：

```bash
freqtrade hyperopt --config user_data/config.json --strategy MyStrategy --spaces buy sell roi stoploss trailing -e 500 -j -1
```

#### 先优化信号，再优化风控

- 第一轮：`buy/sell` 或 `enter/exit`
- 第二轮：`roi/stoploss/trailing`
- 第三轮：`protection`

这样比一次把所有空间全开更容易定位问题。

---

## 8. 模拟盘 / 实盘运行指令集

相关文档来源：

- `docs/zh_CN/机器人用法.md`
- `docs/zh_CN/配置文件说明.md`
- `docs/zh_CN/杠杆交易.md`
- `docs/zh_CN/FreqUI界面.md`

---

### 8.1 启动机器人

#### 基础启动

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy
```

说明：

- 最核心命令
- 是否 dry-run / 实盘，取决于配置中的 `dry_run`

---

### 8.2 启动模拟盘

#### 配置文件里写 `dry_run: true`

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy
```

或命令行临时切 dry-run（如果版本支持对应参数时可用，否则以配置为准）。

实践建议：

- 模拟盘配置和实盘配置分开维护
- 例如：
  - `config_strategy_bt.json`
  - `config_strategy_dry.json`
  - `config_strategy_live.json`

---

### 8.3 启动实盘

前提：

- `dry_run: false`
- 配置好交易所 API key / secret
- 确认交易模式、杠杆、止损、仓位、白名单都已检查

```bash
freqtrade trade --config user_data/config_live.json --strategy MyStrategy
```

---

### 8.4 使用多个配置文件

```bash
freqtrade trade -c user_data/config_base.json -c user_data/config_secrets.json --strategy MyStrategy
```

文档说明：

- 后面的配置会覆盖前面的同名配置

适合：

- 把 API 密钥单独放 secrets 文件
- 把基础配置、环境配置、私密配置分层管理

---

### 8.5 指定数据库

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy --db-url sqlite:///user_data/tradesv3_mybot.sqlite
```

用途：

- 多个机器人分开数据库
- dry-run / live 分库
- 后续分析更清晰

---

### 8.6 指定策略目录

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy --strategy-path user_data/strategies
```

#### 递归搜索策略

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy --recursive-strategy-search
```

适合：

- 策略分类存放在子目录中

---

### 8.7 期货 / 杠杆实盘重点

配置关键项：

```json
"trading_mode": "futures",
"margin_mode": "isolated",
"leverage": 1,
"liquidation_buffer": 0.05
```

文档重点：

- 期货交易对格式：`ETH/USDT:USDT`
- 做空策略必须 `can_short = True`
- 杠杆越大，止损要求越严格
- `liquidation_buffer` 用于避免止损过于接近爆仓价

建议：

- 新策略先 `1x`
- 未验证稳定前不要放大杠杆

---

### 8.8 Web UI / API 运行

#### 只跑 WebServer

```bash
freqtrade webserver --config user_data/config.json
```

#### 跑交易并通过 UI 管理

```bash
freqtrade trade --config user_data/config.json --strategy MyStrategy
```

前提配置示例：

```json
"api_server": {
  "enabled": true,
  "listen_ip_address": "0.0.0.0",
  "listen_port": 8787,
  "username": "admin",
  "password": "your_password_here"
}
```

---

## 9. 实用辅助命令

### 9.1 列出市场

```bash
freqtrade list-markets --config user_data/config.json
```

用途：

- 检查交易所连接
- 检查代理 / 网络是否正常
- 确认交易对命名是否正确

---

### 9.2 查看回测结果目录

回测结果默认在：

- `user_data/backtest_results/`

超参结果默认在：

- `user_data/hyperopt_results/`

建议：

- 定期清理旧结果
- 否则高级分析容易混淆

---

## 10. 结合你当前项目的推荐命令模板

---

### 10.1 下载 1h 期货数据

```bash
freqtrade download-data --config user_data/config_momentum_server_v1.json --timeframes 1h
```

如果要补历史：

```bash
freqtrade download-data --config user_data/config_momentum_server_v1.json --timeframes 1h --timerange 20250101-
```

---

### 10.2 回测某个策略

```bash
freqtrade backtesting --config user_data/backtest_config.json --strategy MomentumFusion_V1 --timerange 20250101-
```

---

### 10.3 提高止损回测精度

```bash
freqtrade backtesting --config user_data/backtest_config.json --strategy MomentumFusion_V1 --timerange 20250101- --timeframe-detail 5m
```

---

### 10.4 导出信号做标签分析

```bash
freqtrade backtesting --config user_data/backtest_config.json --strategy MomentumFusion_V1 --timerange 20250101- --export signals --cache none
```

然后：

```bash
freqtrade backtesting-analysis -c user_data/backtest_config.json --analysis-groups 0 2 3 5
```

---

### 10.5 做一轮 Sharpe 优化

```bash
freqtrade hyperopt --config user_data/backtest_config.json --strategy MomentumFusion_V1 --timerange 20250101- --spaces buy sell roi stoploss --hyperopt-loss SharpeHyperOptLossDaily -e 300 -j -1
```

---

### 10.6 启动模拟盘

```bash
freqtrade trade --config user_data/config_momentum_server_v1.json --strategy MomentumFusion_V1
```

---

## 11. 常见使用误区

### 11.1 回测数据和交易模式不一致

错误示例：

- 策略是 futures
- 数据却只下载了 spot

后果：

- 回测报错或结果异常

---

### 11.2 以为 `--enable-dynamic-pairlist` 能让所有 pairlist 回测

不是。
文档已说明：

- 只有支持该特性的 pairlist handler 才有效

---

### 11.3 Hyperopt 只优化信号，不做样本外验证

后果：

- 回测漂亮
- 实盘失真

建议：

- 样本内优化
- 样本外验证
- 分牛熊震荡阶段验证

---

### 11.4 用过小资金回测 unlimited stake

如果：

- `stake_amount = "unlimited"`
- `max_open_trades` 很大
- `dry_run_wallet` 太小

则结果会失真甚至无法开仓。

---

## 12. 推荐的日常工作流

### 工作流 A：开发新策略

1. `new-strategy`
2. `download-data`
3. `backtesting`
4. `backtesting --export signals`
5. `backtesting-analysis`
6. `hyperopt`
7. 再回测验证
8. 上 dry-run

### 工作流 B：迭代现有策略

1. 补数据
2. 固定 timerange 回测
3. 对比旧版本
4. 分标签分析
5. 小规模 hyperopt
6. dry-run 观察

### 工作流 C：上线前检查

1. 检查 config 是否 `dry_run: false`
2. 检查 API key
3. 检查白名单 / 黑名单
4. 检查 `trading_mode` / `margin_mode`
5. 检查 `stoploss` / `leverage`
6. 检查数据库和日志路径
7. 先小仓位实盘

---

## 13. 文档来源索引

本整理主要参考以下中文文档：

- `docs/zh_CN/回测指南.md`
- `docs/zh_CN/高级回测分析.md`
- `docs/zh_CN/超参数优化.md`
- `docs/zh_CN/数据下载.md`
- `docs/zh_CN/机器人用法.md`
- `docs/zh_CN/配置文件说明.md`
- `docs/zh_CN/杠杆交易.md`
- `docs/zh_CN/FreqUI界面.md`
- `docs/zh_CN/freqtrade核心命令实用手册.md`
- `docs/commands/backtesting.md`

---

如果后续你愿意，可以继续在这个文件基础上再拆两版：

1. **你的项目专用命令版**：只保留 `MomentumFusion_*` 和 `user_data/*.json` 相关命令
2. **排错版**：专门整理“为什么回测失败 / 为什么没有交易 / 为什么 hyperopt 不生效”的诊断命令
