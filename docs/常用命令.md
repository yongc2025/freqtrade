## 常用命令

- 1.git stash → 把改动放进抽屉（本地）

- 2.git pull origin develop → 拉取我推的修复代码

- 3.git stash pop → 把你的改动从抽屉里拿出来

- 4.git log --oneline -5 查看最近5次提交

## 设置代理端口

- $env:HTTP_PROXY="http://127.0.0.1:10808"
- $env:HTTPS_PROXY="http://127.0.0.1:10808"
