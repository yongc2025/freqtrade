---
services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade
    volumes:
      - "./user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "0.0.0.0:8787:8787"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3_momentum_live.sqlite
      --config /freqtrade/user_data/config_momentum_server_dynamic.json
      --strategy StabilityMasterV4_MomentumFusion
  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile.dashboard
    container_name: freqtrade-dashboard
    restart: unless-stopped
    working_dir: /app
    volumes:
      - "./user_data:/app/user_data"
    ports:
      - "0.0.0.0:8788:8788"
    command: >
      python scripts/dashboard/server.py user_data/tradesv3.sqlite 950 8788
